{
  "name": "lson-cli",
  "version": "0.0.6",
  "description": "lson cli",
  "main": "build/index.js",
  "bin": {
    "lson": "bin/lson"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "lsc -cbo build src/*.ls",
    "prepublish": "rm -rf build && npm run build"
  },
  "author": "jigsaw (http://jgs.me)",
  "license": "MIT",
  "dependencies": {
    "@jigsaw/lson": "^2.0.2",
    "glob": "^5.0.14",
    "minimist": "^1.1.1"
  },
  "devDependencies": {
    "livescript": "^1.4.0"
  }
}
